# Agent Development Rules

本文件是Agent端的长期协作规则。在Agent开发时必须优先遵守。

## 交流与执行

- 当用户在对话中提出新的长期规范、流程约束、代码组织规则或协作规则时，应自动补充到本文件；除非用户明确说只适用于当前一次任务。
- 本阶段任务文档`D:\code\AICareDesk\aicare-desk-agent\stage-4.md`，该文档应该规划整个阶段的任务，在执行具体某个任务的时候再规划成若干子任务。`D:\code\AICareDesk\阶段性文档.md`是每个Task结束后在本阶段记录该任务的完成情况。
- 后续新增运行能力必须直接采用生产级实现和生产级外部依赖，默认失败即阻断，不新增内存持久化、演示型业务实现、静默回退或与生产逻辑并行的开发版功能。
- Java契约变更已经同步回本体；后续如需修改Java代码，直接修改`D:\code\AICareDesk\aicare-desk-platform-api`
- Python Agent 后续新增或修改源码时，保留必要的关键详细注释：**方法**内部仅按业务执行顺序使用“1、2、3、……”说明关键步骤，不必同时解释重要参数、返回值、异常边界和不直观语法，**类**说明类的作用，字段含义，**文件**说明该文件的作用
- 涉及模型、Prompt、结构化路由或LangGraph图行为的任务，最终验收必须显式执行真实DeepSeek调用并回查LangSmith Trace，核验run name、标签、元数据和敏感信息脱敏；离线确定性测试继续覆盖安全、身份、阈值与故障分支，但不得替代真实模型验收。

## 本地开发

- Agent开发任务全部实现并通过相应验证后，直接在本仓库创建提交并推送到本仓库`origin`。
- 子任务或阶段尚未完成、验证失败、存在待用户确认的方案时不得提前提交或推送；先汇报阻塞或等待确认。

- 开发默认使用虚拟机docker容器，地址：`192.168.150.105`。
- 各种密码位于D:\code\AICareDesk\阶段性文档.md，可直接读取，开发无安全隐患;虚拟机有新增的组件等把对应的密码补充到`D:\code\AICareDesk\阶段性文档.md`开始的表中。
